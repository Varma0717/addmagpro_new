@extends('layouts.master')
@section('page_title', 'Store Images List | AddMagPro Admin')
@section('mainsection')
<div class="admin-page"><section class="admin-section"><div class="admin-section-body"><div class="mb-4"><h2 class="admin-page-title">Store Images List</h2><p class="admin-page-subtitle">Speciality store image records.</p></div><div class="card admin-card"><div class="card-header admin-card-header"><h4>Store Images List</h4></div><div class="card-body admin-card-body p-0"><div class="table-responsive admin-table-wrap"><table class="table admin-table align-middle mb-0"><thead><tr><th>#</th><th>Store Name</th><th>Image</th><th>Action</th></tr></thead><tbody>@forelse($gallery_detail as $index => $item)<tr><td>{{ method_exists($gallery_detail, 'firstItem') ? $gallery_detail->firstItem() + $index : $index + 1 }}</td><td class="fw-semibold text-dark">{{ $item->store_name ?? '-' }}</td><td>@if(!empty($item->store_image))<img src="{{ asset($item->store_image) }}" class="admin-thumb" alt="Store image">@else - @endif</td><td><a href="{{ route('update_store_gallery', ['gallery_id' => $item->store_id]) }}" class="btn btn-theme btn-sm">Update</a></td></tr>@empty<tr><td colspan="4" class="text-center text-muted py-4">No store image records found.</td></tr>@endforelse</tbody></table></div></div><div class="card-footer admin-card-footer d-flex justify-content-end">{{ $gallery_detail->links() }}</div></div></div></section></div>
@endsection
